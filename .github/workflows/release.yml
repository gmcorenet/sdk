name: Build Framework Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.21'

      - name: Create directories
        run: |
          mkdir -p vendor/gmcore
          for dir in gmcore-*/; do
            if [ -d "$dir" ] && [ -f "$dir/go.mod" ]; then
              rm -rf "vendor/gmcore/$(basename $dir)"
              cp -r "$dir" vendor/gmcore/
            fi
          done

      - name: Create framework structure
        run: |
          VERSION=${GITHUB_REF#refs/tags/}
          FRAMEWORK="gmcore-framework-${VERSION}"
          mkdir -p "$FRAMEWORK"/{bin,cmd/app,config,public,var/{cache,log,tmp},src,tests,migrations,templates,vendor/gmcore}
          cp -r vendor/gmcore/* "$FRAMEWORK/vendor/gmcore/"

      - name: Create go.mod
        run: |
          VERSION=${GITHUB_REF#refs/tags/}
          FRAMEWORK="gmcore-framework-${VERSION}"
          cat > "$FRAMEWORK/go.mod" << 'GOMODEOF'
          module github.com/gmcore/app

          go 1.21

          require (
          	gmcore.io/gmcore-config vVERSION
          	gmcore.io/gmcore-crud vVERSION
          	gmcore.io/gmcore-encryption vVERSION
          	gmcore.io/gmcore-events vVERSION
          	gmcore.io/gmcore-router vVERSION
          	gmcore.io/gmcore-settings vVERSION
          	gmcore.io/gmcore-store vVERSION
          	gmcore.io/gmcore-uuid vVERSION
          )

          replace (
          	gmcore.io/gmcore-config => ./vendor/gmcore/gmcore-config
          	gmcore.io/gmcore-crud => ./vendor/gmcore/gmcore-crud
          	gmcore.io/gmcore-encryption => ./vendor/gmcore/gmcore-encryption
          	gmcore.io/gmcore-events => ./vendor/gmcore/gmcore-events
          	gmcore.io/gmcore-router => ./vendor/gmcore/gmcore-router
          	gmcore.io/gmcore-settings => ./vendor/gmcore/gmcore-settings
          	gmcore.io/gmcore-store => ./vendor/gmcore/gmcore-store
          	gmcore.io/gmcore-uuid => ./vendor/gmcore/gmcore-uuid
          )
          GOMODEOF
          sed -i "s/vVERSION/v${VERSION}/g" "$FRAMEWORK/go.mod"

      - name: Build tarball
        run: |
          VERSION=${GITHUB_REF#refs/tags/}
          FRAMEWORK="gmcore-framework-${VERSION}"
          tar -czf "gmcore-framework-${VERSION}.tar.gz" "$FRAMEWORK"

      - name: Upload Release
        uses: softprops/action-gh-release@v2
        with:
          files: gmcore-framework-*.tar.gz
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
